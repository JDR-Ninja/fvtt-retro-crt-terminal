{
  "name": "retro-crt-terminal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/dev/build-release.mjs",
    "build:packs": "node scripts/dev/build-packs.mjs",
    "test": "node --test",
    "check": "node scripts/dev/check-syntax.mjs"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^3.0.4",
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": ">=20"
  }
}
