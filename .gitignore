node_modules/
dist/
release/
coverage/
*.tgz

# Compiled Compendium packs are build output: `npm run build:packs` produces them from
# packs-src/ for development, and the release build compiles them again into dist/.
packs/
